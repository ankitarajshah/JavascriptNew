function transformToObjects() {
  // Todo: Add your logic
  // should return an array of objects
  let arr = [1, 2, 3];
  const arr2 = arr.map((value) => ({ val: value }));
  console.log(arr2);
  return;
}
transformToObjects();
